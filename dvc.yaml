


metrics:
  - metrics.json

params:
  - params.yaml

plots:
  - plot.csv:
      y: accuracy
      x: epoch

stages:
  calculate:
    cmd: python3 -u cca_analysis.py
    params:
      - cache
      - cca
    outs:
      - results/cca_scores.csv