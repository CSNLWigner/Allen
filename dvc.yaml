


metrics:
  - metrics.json

params:
  - params.yaml

plots:
  - plot.csv:
      y: accuracy
      x: epoch

stages:
  analysis:
    cmd: python3 -u cca_analysis.py
    params:
      - cache
      - cca
    outs:
      - results/cca_scores.csv
  plots:
    cmd: python3 -u cca_plot.py
    deps:
      - results/cca_scores.csv
    params:
      - cca